/*describe('Feedback Controller', function() { 
  
  var $controllerConstructor, scope;

  beforeEach(module('ui.router'));
  beforeEach(module('budgetrentacar.feedback'));
  beforeEach(inject(function($controller, $rootScope){
    $controllerConstructor = $controller;
    scope = $rootScope.$new();
  }));

  it('should have 6 items', function() {
    var ctrl = $controllerConstructor('FeedbackController', {$scope: scope});
    expect(Object.keys(ctrl.feedback).length).toBe(6);
  }); 

  

   
});*/
